import palette from './palette';

export const checkboxStyleOverrides = {
  styleOverrides: {
    root: {
      color: palette.primary.main,
      '&:hover': {
        color: palette.primary.main,
      },
    },
  },
};
